# Documentation Index

This repository keeps detailed public docs under `docs/`.

## Start here
- `docs/USER_GUIDE.md` for setup and day-to-day usage.
- `docs/API.md` for the `UHF_*` DLL interface.
- `docs/CLI.md` for command-line usage and options.

## Technical references
- `docs/ARCHITECTURE.md` for wrapper layering and behavior.
- `docs/STRUCTURE.md` for repository layout and component mapping.

## Policies and support
- `SECURITY.md` for vulnerability reporting.
- `SUPPORT.md` for usage help and bug report channels.
- `NOTICE_VENDOR.md` for vendor DLL distribution and licensing boundary.
